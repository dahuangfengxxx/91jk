# Netlify配置文件 - 平衡安全性和功能性

# 临时允许CSV文件访问以确保网站功能正常
# 但添加安全头部限制批量下载

# 保护敏感文件
[[redirects]]
  from = "/*.py"
  to = "/404.html"
  status = 404
  force = true

[[redirects]]
  from = "/__pycache__/*"
  to = "/404.html"
  status = 404
  force = true

[[redirects]]
  from = "/scripts/*"
  to = "/404.html"
  status = 404
  force = true

# 主页面设置
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

# HTTP安全头部设置
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    
[[headers]]
  for = "/data/*.json"
  [headers.values]
    Cache-Control = "public, max-age=3600"
    X-Robots-Tag = "noindex, nofollow"
    Content-Security-Policy = "default-src 'none'; script-src 'unsafe-inline' 'unsafe-eval'"

[[headers]]
  for = "/*.csv"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"
    Cache-Control = "private, no-cache, no-store, must-revalidate"
    Pragma = "no-cache"
    Expires = "0"
    Content-Disposition = "inline; filename=data.txt"

# 构建设置
[build]
  publish = "."
  command = "echo 'Build completed'"

# 插件配置 - 安全扫描
[[plugins]]
  package = "@netlify/plugin-lighthouse"

# 环境变量（在Netlify面板中设置）
# SITE_SECURITY_KEY=your-secret-key
# ALLOWED_DOMAINS=91jk.liyang2002.com,localhost